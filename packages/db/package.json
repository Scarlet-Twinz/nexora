{
  "name": "@nexora/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "migrate": "prisma migrate dev --name init",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
